plugins {
    id 'com.android.application'
}

android {
    namespace 'com.codmash.pphjobs'
    compileSdk 35

    defaultConfig {
        applicationId "com.codmash.pphjobs.notify"
        minSdk 24
        targetSdk 35
        versionCode 3
        versionName "1.2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
